name: diario_de_bordo_app
description: Aplicativo móvel de ponto eletrônico e diário de bordo.
publish_to: "none"
version: 0.4.0+4

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  geolocator: ^14.0.3
  http: ^1.5.0
  image_picker: ^1.2.3
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
